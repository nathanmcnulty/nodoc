components:
  responses:
    Unauthorized:
      description: Authentication is missing, expired, or invalid for the Defender portal session.
    Forbidden:
      description: The signed-in operator does not have access to the requested tenant or workload.
    NotFound:
      description: The requested MTO resource or wrapped Defender resource was not found.

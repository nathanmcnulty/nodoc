paths:
  /api/AppManagement/TenantProducts:
    servers:
      - url: "https://api.admin.powerplatform.microsoft.com"
        description: "Power Platform admin portal backend"
    get:
      operationId: AdminPortal_get_api_AppManagement_TenantProducts
      summary: "GET /api/AppManagement/TenantProducts"
      description: "Live-captured from the authenticated Power Platform admin center while browsing: https://admin.powerplatform.microsoft.com/environments/621c0f33-3fd1-f011-83fb-000d3a106b3b/applications, https://admin.powerplatform.microsoft.com/environments/environment/Default-847b5907-ca15-40f4-b171-eb18619dbfab/hub."
      tags:
        - AdminPortal
      parameters:
        - name: "geoType"
          in: query
          required: false
          schema:
            type: string
            example: "Na"
      responses:
        '200':
          description: "200 captured response"
  /api/environments/{environmentsId}/features:
    servers:
      - url: "https://api.admin.powerplatform.microsoft.com"
        description: "Power Platform admin portal backend"
    get:
      operationId: AdminPortal_get_api_environments_environmentsId_features
      summary: "GET /api/environments/{environmentsId}/features"
      description: "Live-captured from the authenticated Power Platform admin center while browsing: https://admin.powerplatform.microsoft.com/environments/environment/Default-847b5907-ca15-40f4-b171-eb18619dbfab/hub."
      tags:
        - AdminPortal
      parameters:
        - name: "environmentsId"
          in: path
          required: true
          schema:
            type: string
            format: uuid
            example: "621c0f33-3fd1-f011-83fb-000d3a106b3b"
        - name: "geo"
          in: query
          required: false
          schema:
            type: string
            example: "Na"
      responses:
        '200':
          description: "200 captured response"
  /api/health/ping:
    servers:
      - url: "https://api.admin.powerplatform.microsoft.com"
        description: "Power Platform admin portal backend"
    get:
      operationId: AdminPortal_get_api_health_ping
      summary: "GET /api/health/ping"
      description: "Live-captured from the authenticated Power Platform admin center while browsing: https://admin.powerplatform.microsoft.com/billing/licenses/PowerApps/overview, https://admin.powerplatform.microsoft.com/billing/licenses/PowerApps/recommendations, https://admin.powerplatform.microsoft.com/billing/licenses/copilotStudio/overview, https://admin.powerplatform.microsoft.com/billing/licenses/dataverse/overview, and 83 more routes."
      tags:
        - AdminPortal
      security: []
      responses:
        '200':
          description: "200 captured response"
          content:
            text/plain:
              schema:
                type: string
                example: "This action is to redirect legacy routes"
